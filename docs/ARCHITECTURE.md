# Архитектура GOART

## Текущая архитектура

Сейчас проект устроен максимально просто:

```text
GOART/
├── index.html
├── main.js
├── README.md
└── PROJECT_NOTES_AWB_GOART.md
```

`index.html` содержит:

- HTML-разметку;
- встроенные CSS-стили;
- основные DOM-контейнеры;
- подключение `main.js`.

`main.js` содержит почти всю игровую логику:

- данные комнат;
- данные NPC;
- квесты;
- состояние игры;
- утилиты;
- спавн NPC;
- движение NPC;
- старт и сброс игры;
- рендер карты;
- рендер квестов;
- рендер футера;
- обработку перемещений;
- действия в комнатах;
- обработку встреч с NPC;
- модальные окна событий;
- финальный блиц босса;
- справку.

## Основные DOM-элементы

В `index.html` используются:

```text
#header
#main-wrap
#map
#questlog
#footer
#eventBox
#eventPortrait
#eventText
#eventOptions
#descModal
#legendCont
```

Код в `main.js` должен обращаться только к тем DOM-элементам, которые реально существуют в `index.html`.

## Важное правило стабильности

Перед обращением к DOM-элементу желательно либо гарантировать его наличие в HTML, либо использовать защитную проверку:

```js
const el = document.getElementById('some-id');
if (el) {
  el.innerText = 'Текст';
}
```

Это особенно важно для проекта, который часто меняется через генерацию и ручные правки.

## Текущие архитектурные проблемы

1. Весь код находится в одном большом `main.js`.
2. CSS встроен в `index.html`.
3. Есть дублирование функции `shuffle()`.
4. Нет отдельного слоя данных, рендера и игровой логики.
5. Не хватает автоматической проверки целостности данных.
6. Нет тестового режима для быстрой проверки квестов и финала.

## Рекомендуемая будущая структура

После исправления критических ошибок можно постепенно перейти к структуре:

```text
GOART/
├── index.html
├── README.md
├── PROJECT_NOTES_AWB_GOART.md
├── docs/
│   ├── PROJECT_OVERVIEW.md
│   ├── ARCHITECTURE.md
│   ├── GAME_DESIGN.md
│   ├── ROADMAP.md
│   └── KNOWN_BUGS.md
├── css/
│   └── style.css
├── js/
│   ├── data.rooms.js
│   ├── data.npcs.js
│   ├── data.quests.js
│   ├── state.js
│   ├── utils.js
│   ├── npc.logic.js
│   ├── quests.logic.js
│   ├── render.js
│   ├── events.js
│   ├── boss.quiz.js
│   └── main.js
└── img/
```

## Рекомендуемый порядок рефакторинга

1. Сначала исправить ошибки запуска в текущем виде.
2. Зафиксировать рабочую версию коммитом.
3. Создать резервную ветку или тег релиза.
4. Только потом переносить CSS в отдельный файл.
5. Затем отделить данные от логики.
6. Затем отделить рендер от игровой логики.

## Почему не нужно сразу переписывать всё

Проект уже имеет рабочую игровую идею, карту и данные. Главный риск — потерять текущую логику при слишком резком рефакторинге.

Правильный подход:

```text
сначала стабилизация → потом документация → потом маленькие патчи → потом рефакторинг
```
