// game.events.js
function checkEvents(){
  // ... Как раньше: логика квестов, событий, встреч с NPC ...
}
// showEvent, showEventNPC — тоже сюда!
function showEventNPC(text, opts, npc){/* ... */}
function showEvent(text, opts){/* ... */}
window.eventAction = function(idx){/* ... */}
